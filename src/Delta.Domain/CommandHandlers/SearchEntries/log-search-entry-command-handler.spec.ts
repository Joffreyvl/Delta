import { LogSearchEntryCommandHandler } from './log-search-entry-command-handler';

describe('LogSearchEntryCommandHandler', () => {
  it('should be defined', () => {
    expect(new LogSearchEntryCommandHandler()).toBeDefined();
  });
});
